<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/logo_clar"
                android:centerColor="@color/logo_clar"
                android:centerY="1.0"
                android:endColor="@color/logo_clar"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/complementari_logo_fosc"
                    android:centerColor="@color/complementari_logo_fosc"
                    android:centerY="1.0"
                    android:endColor="@color/complementari_logo_fosc"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>